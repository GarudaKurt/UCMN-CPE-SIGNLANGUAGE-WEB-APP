# ──────────────────────────────────────────────
# UCMN-CPE-SIGNLANGUAGE-WEB-APP — requirements.txt
# ──────────────────────────────────────────────

# Web framework
flask>=3.0.0

# Computer vision — camera capture & frame encoding
opencv-python>=4.9.0

# Hand landmark detection (MediaPipe)
mediapipe>=0.10.0

# Machine-learning models (joblib-serialised classifiers)
scikit-learn>=1.4.0
joblib>=1.3.0

# Numerical computing (used throughout fsl_worker)
numpy>=1.26.0

# Speech recognition (microphone → Google STT)
SpeechRecognition>=3.10.0

# Audio I/O backend required by SpeechRecognition
pyaudio>=0.2.14

# ──────────────────────────────────────────────
# Standard-library modules used (no install needed):
#   threading, queue, json, base64, time,
#   os, urllib.request, collections, warnings, subprocess
# ──────────────────────────────────────────────
